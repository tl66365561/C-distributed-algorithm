Raft 算法是一种分布式共识算法，用于多个节点之间达成一致的状态。
它将系统中的节点分为三类：Leader、Follower 和 Candidate，其中 Leader 作为系统的领导者，负责维护整个系统的状态，Follower 和 Candidate 则作为从属节点，接收 Leader 的指令并执行。在 Raft 算法中，所有节点均由相同的状态机控制，并使用日志来记录状态变化。通过不断地进行选举和发送心跳消息，Raft 算法确保了整个系统的高可用性和数据一致性。